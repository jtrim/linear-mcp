query GetTeamProjects($teamId: String!) {
  team(id: $teamId) {
    projects {
      nodes {
        id
        name
        slugId
        status {
          id
          name
        }
      }
    }
  }
}